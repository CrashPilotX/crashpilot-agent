apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: crashpilot

resources:
  - namespace.yaml
  - clusterrole.yaml
  - configmap.yaml
  - daemonset.yaml

# To set your API key without editing configmap.yaml:
#
# secretGenerator:
#   - name: crashpilot-secrets
#     envs:
#       - secrets.env   # file with CRASHPILOT_ANTHROPIC_API_KEY=sk-ant-...
#     options:
#       disableNameSuffixHash: true
